/**
 * EXERCISE 1: Sum an array using a for loop
 *
 * Write a loop that adds every number in `data` and stores the result in `total`.
 * Do NOT use .reduce() — practice the accumulator pattern!
 */

const data = [4, 8, 15, 16, 23, 42];

let total = 0;
// TODO: your for loop here


console.log(`Total should be 108, got: ${total}`);
